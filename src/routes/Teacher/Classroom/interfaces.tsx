// types for class data when getting from the server
export interface IClassData {
  id: string;
  name: string;
  description: string;
  line: string;
  falcuty: string;
  avatarData: string;
  avatar?: string;
  Id: string;
  lineId?: string;
  falcutyId?: string;
}
