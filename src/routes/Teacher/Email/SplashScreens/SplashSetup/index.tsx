import React from "react";

const SplashSetup = () => (
  <div
    style={{ display: "flex", alignItems: "center", justifyContent: "center" }}
  >
    <h1>Setting up mailbox ...</h1>
  </div>
);

export default SplashSetup;
